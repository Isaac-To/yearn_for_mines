## 1. Unified Perception & Formatting

- [x] 1.1 Refactor `observation-formatter.ts` to output the new condensed "Context Frame" format instead of verbose lists.
- [x] 1.2 Modify `observation-builder.ts` to summarize nearby entities/blocks into the top 5 "Points of Interest" by proximity.
- [x] 1.3 Ensure inventory data is summarized into `Record<ItemName, Count>` rather than returning 36 slots.
- [x] 1.4 Wire recent damage and chat events into the Context Frame structure.

## 2. MCP Server Macro-Skill Implementation

- [x] 2.1 Remove the existing micro-tools in `packages/mc-mcp-server/src/tools/` (`look_at`, `pathfind_to`, `dig_block`, `drop_item`, etc.) - excluding pure lifecycle `bot_connect`, `bot_status`.
- [x] 2.2 Create `gather_materials` macro-skill with Mineflayer while-loop logic (find, equip, walk, dig, wait for collection), returning a Context Frame on finish or failure.
- [x] 2.3 Create `craft_items` macro-skill to handle inventory checks, crafting table placement (if needed), recipe execution, and block breaking. Returns Context Frame.
- [x] 2.4 Create `reposition` macro-skill to handle autonomous pathfinding relative to targets or coordinates. Returns Context Frame.
- [x] 2.5 Create `combat` macro-skill to handle finding the nearest target, equipping weapons, closing distance, and attacking until resolution. Returns Context Frame.
- [x] 2.6 Create `interact` macro-skill for interacting with single items in the environment or active hand (like beds, doors, eating food). Returns Context Frame.

## 3. Agent Loop Refactor

- [ ] 3.1 Modify `agent-loop.ts` to remove the dedicated explicit `perceive()` phase.
- [ ] 3.2 Initialize the SLM prompt context using the Context Frame generated by an initial internal `bot_status` call instead of calling `observe`.
- [ ] 3.3 Ensure the tool execution phase captures the Context Frame return from the MCP and directly uses it as the `observation` payload for subsequent `plan()` phases during the while loop.
- [ ] 3.4 Adjust agent loop retry logic and timeouts to support asynchronous tasks taking 15+ seconds.- [x] 2.7 Create `build` macro-skill to handle block placement, checking for valid faces and proximity. Returns Context Frame.
