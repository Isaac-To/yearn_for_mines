FROM python:3.12-slim

RUN apt-get update && \
    apt-get install -y --no-install-recommends curl && \
    rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir mempalace chromadb

EXPOSE 8080

CMD ["mempalace", "serve", "--transport", "streamable-http", "--port", "8080"]